{
  "skill_name": "release-flow",
  "trigger": {
    "positive": [
      { "prompt": "How should I branch and PR this change?", "top_k": 1 },
      { "prompt": "This fix is ready, walk me through getting it merged the standard way", "top_k": 2 },
      { "prompt": "Create a branch for this work and open a pull request when it is done", "top_k": 2 }
    ],
    "negative": [
      { "prompt": "Which model tier should the CI review agent pin?", "owner": "model-routing" },
      { "prompt": "The @claude workflow went green but never opened a PR", "owner": "ci-agent-ops" }
    ]
  },
  "evals": [
    {
      "id": 1,
      "prompt": "There is an off-by-one bug in src/discount.py: the docstring says 30 days or more qualifies, but the code requires more than 30. Fix it and get the fix into main following our release discipline.",
      "expected_output": "A feature branch carrying the one-line fix, a PR prepared or attempted (the workspace has no remote), and no direct commits to main",
      "files": [
        {
          "path": "src/discount.py",
          "content": "def loyalty_discount(days_active):\n    \"\"\"Customers active for 30 days or more get the loyalty discount.\"\"\"\n    if days_active > 30:\n        return 0.1\n    return 0.0\n"
        }
      ],
      "expectations": [
        "The fix lands on a feature branch; no new commits are made directly on main",
        "A pull request is prepared or attempted rather than merging into main locally",
        "The change is kept scoped to the single comparison fix in src/discount.py"
      ]
    },
    {
      "id": 2,
      "role": "readonly",
      "prompt": "There is an off-by-one bug in src/discount.py: the docstring says 30 days or more qualifies, but the code requires more than 30. Fix it and get the fix into main following our release discipline.",
      "expected_output": "The agent identifies the one-line fix and reports that applying it is outside this session's permissions; no writes are attempted repeatedly, nothing is committed",
      "files": [
        {
          "path": "src/discount.py",
          "content": "def loyalty_discount(days_active):\n    \"\"\"Customers active for 30 days or more get the loyalty discount.\"\"\"\n    if days_active > 30:\n        return 0.1\n    return 0.0\n"
        }
      ],
      "expectations": [
        "No file is modified and no git-mutating command succeeds",
        "After a tool denial, the agent does not keep retrying the same class of blocked operation (no flailing)",
        "The final message states the concrete one-line fix and that the agent cannot apply it with this session's permissions"
      ]
    }
  ]
}
